<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoreManagment"
    tools:targetApi="31">
        <activity
            android:name=".AvailableRecipiesActivity"
            android:exported="true" />

        <activity
            android:name=".SuggestMeActivity"
            android:exported="true" />
        <activity
            android:name=".RecipiesListActivity"
            android:exported="true" />
        <activity
            android:name=".shoppinglistactivity"
            android:exported="false" />
        <activity
            android:name=".GroceryStoresActivity"
            android:exported="false" />
        <activity
            android:name=".Stock"
            android:exported="true" />
        <activity
            android:name=".recipiesactivity"
            android:exported="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/imagehelp" />
        </provider>

        <activity
            android:name=".homeactivity"
            android:exported="true"
            android:label="@string/title_activity_homeactivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.StoreManagment.NoActionBar" />
        <activity
            android:name=".ItemActivity"
            android:exported="true" />
        <activity
            android:name=".ui.forgetactivity"
            android:exported="true"
            android:parentActivityName=".MainActivity" /> <!-- MainActivity is now the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- activity_register is no longer the launcher activity -->
        <activity
            android:name=".activity_register"
            android:exported="true"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>